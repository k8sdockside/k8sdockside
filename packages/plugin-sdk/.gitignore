# Copied from internal/plugins/sdk by scripts/sync.mjs at pack time.
/k8sdockside.d.ts
/node_modules/
*.tgz

# The repository's .gitignore ignores every bin/ (build output); this one is
# the package's command.
!/bin/
